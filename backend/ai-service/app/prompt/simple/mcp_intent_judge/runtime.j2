### 当前系统时间
-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

### 判断指南

请严格按照以下标准判断：

#### Step 1：分析用户需求
- 用户是否要求执行可被工具自动化的操作？
- 用户是否在询问需要外部数据或信息的问题？
- 用户是否在要求处理、转换、分析某些内容？

#### Step 2：判断标准
以下情况需要 MCP 能力（need_skill=true）：
- 明确要求"查询"、"搜索"、"查找"某些信息
- 要求"翻译"、"转换"、"计算"、"分析"
- 要求"处理文件"、"读取文档"
- 要求"生成"、"创建"某些内容

以下情况不需要 MCP 能力（need_skill=false）：
- 纯闲聊、问候、情感交流
- 只需要使用已有知识回答的问题
- 要求改变自己的行为或角色设定
- 感谢、告别等社交性对话

#### Step 3：提炼 MCP 意图
如果 need_skill=true，将用户需求重构为简洁明确的工具操作描述。
例如：
- 用户说"帮我看看这个文件里写了什么" → mcp_intent: "读取并解析指定文件的内容"
- 用户说"搜索一下关于AI的最新论文" → mcp_intent: "搜索人工智能领域的最新论文信息"

---

### 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

#### C1：需求分析校验
1. **用户意图理解**：是否准确理解了用户的真实需求？有没有将闲聊误判为工具需求？
2. **边界判断**：是否正确区分了"需要外部数据"和"已有知识可回答"的场景？

#### C2：判断准确性校验
1. **need_skill 合理性**：need_skill 的判断是否与 Step 2 的判断标准一致？
2. **关键词质量**：keywords 是否精准反映了用户需求的核心概念？有没有过于宽泛或过于狭窄？

#### C3：意图提炼校验（need_skill=true 时）
1. **简洁准确**：mcp_intent 是否简洁明确地重构了用户需求？有没有遗漏关键约束条件？
2. **可执行性**：mcp_intent 是否足够具体，能让下游技能筛选器准确匹配？

### ⚠️ 严格输出格式要求 ⚠️

你必须严格输出以下 JSON 结构，必须合法。
输出必须是一个合法的 JSON 对象，**禁止**包含任何额外的解释性文本、Markdown 代码块标记或注释。

```json
{
  "check": "[需求分析] 用户意图理解：... 边界判断：... [判断准确性] need_skill合理性：... 关键词质量：... [意图提炼] 简洁准确：... 可执行性：...",
  "need_skill": "布尔值。true 表示需要使用 MCP 能力；false 表示无需使用。",
  "reason": "判断原因说明，详细解释为什么需要或不需要 MCP 能力。",
  "keywords": "关键词数组，用于匹配技能。至少包含 1 个，建议 1~5 个关键词。当 need_skill=false 时为空数组 []。",
  "mcp_intent": "MCP 意图文本。当 need_skill=true 时，为重构后的需求描述；当 need_skill=false 时，此字段为空字符串 \"\"。"
}
```
