【退回上下文 — 第 {{ FALLBACK_COUNT }} 次退回】

本次执行发现当前选中的技能不足以完成任务，需要退回重新筛选技能。
以下是本次执行的全量快照，包含原执行计划和各步骤的执行结果：

### 执行快照（原计划 + 执行结果）
{% if EXECUTION_SNAPSHOT %}
{% for key, state in EXECUTION_SNAPSHOT.items() %}
#### {{ key }}
- 技能：{{ state.skill }}
- 资源：{{ state.resource | default('无') }}
- 工具：{{ state.tools | default('无') }}
- 状态：{{ state.status }}
- 结果：{{ state.result }}
{% endfor %}
{% else %}
（无执行快照信息）
{% endif %}

### 用户原始输入
{{ USER_INPUT }}

### 输入重构判定
是否需要技能：{{ SKILL_NEED_TOOL }}
判定原因：{{ SKILL_REASON }}
检索关键词：{{ SKILL_KEYWORDS }}

请基于以上信息重新筛选技能。注意：
1. 分析执行快照中哪些步骤执行失败或结果不满足需求，避免选择已失败步骤对应的技能。
2. 优先选择能力范围更匹配缺失需求的技能。
3. 如果确实没有合适的技能，请将 no_suitable_skill 设为 true。
