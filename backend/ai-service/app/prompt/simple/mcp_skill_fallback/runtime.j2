### 退回后技能初筛详细指南

（本 Agent 的定位与 Skill 初筛一致，但增加了对旧执行计划的上下文分析。）

请严格按照以下步骤和方法，基于退回上下文重新执行 Skill 初筛决策：

#### Step 1：分析退回原因
- 分析【执行快照】中各步骤的 `status` 和 `result`，识别哪些步骤执行失败或结果不满足需求。
- 理解本次退回的核心原因：是技能能力不足（选错了技能），还是技能能力正确但执行资源缺失？
- 明确哪些已获取的信息可以复用，哪些缺失的信息需要新的技能来补足。

#### Step 2：理解用户需求
- 分析【用户输入】中的核心需求。
- 参考【输入重构判定】中的 `SKILL_NEED_TOOL`、`SKILL_REASON`、`SKILL_KEYWORDS`。
- 如果 `SKILL_NEED_TOOL` 为 `false`，直接判定 `no_suitable_skill=true`。

#### Step 3：逐一评估候选技能（带退回规避）
对【候选技能列表】中的每个 Skill，除了常规评估维度外，额外检查：

1. **历史回避**：这个 Skill 是否在上次执行计划中被选中并导致失败？如果是，降低其优先级（除非退回原因不是技能问题）。
2. **能力覆盖度**：技能描述是否覆盖了执行快照中被标记为"失败"或"未执行"的步骤的需求？
3. **精准匹配度**：技能是否有明确的关键词与 `SKILL_KEYWORDS` 匹配？
4. **互补性**：如果选择多个技能，它们是否能互补覆盖上次失败和本次需求的完整能力链？

#### Step 4：技能选择优先级规则

| 场景 | 选择策略 |
|------|----------|
| 单个技能完全覆盖用户需求且未被上次执行拒绝 | `selected_skill_ids` 仅包含一个元素 |
| 多个技能组合可覆盖完整需求链 | 按执行顺序的优先级降序排列（先执行的在前） |
| 有高度重叠的技能 | 选择描述最精准、与被判定关键词最相关的技能 |
| 所有候选技能都已被尝试过且失败 | `no_suitable_skill=true`，进入最终失败处理 |
| 没有任何技能匹配 | `no_suitable_skill=true`，`selected_skill_ids` 设为空列表 |
| `SKILL_NEED_TOOL=false` | `no_suitable_skill=true`，`selected_skill_ids` 设为空列表 |

#### Step 5：最终校验
- 所有选中的 `skill_id` 必须严格来自【候选技能列表】，**绝对禁止虚构技能名称**。
- 如果 `no_suitable_skill=true`，`selected_skill_ids` **必须为空列表**。
- 如果 `selected_skill_ids` 为空列表但 `no_suitable_skill=false`，这个组合是无效的。
- 在 `reasoning` 中必须引用执行快照中的具体失败步骤来支撑决策理由。

---

### ⚠️ 严格枚举约束与输出格式要求 ⚠️

你必须严格输出以下 JSON 结构。
【致命约束】：所有字段值必须严格遵循字段描述中的约束范围，禁止自行编造！
输出必须是一个合法的 JSON 对象，**禁止**包含任何额外的解释性文本、Markdown 代码块标记或注释。

```json
{
  "selected_skill_ids": [
    "选中的 Skill ID 列表，按优先级排序。\n每个元素必须是候选技能列表中的 skill_id 值。\n最多支持 3 个 Skill 组合。\n如果没有任何技能匹配或无需技能，此列表必须为空 []。"
  ],
  "reasoning": "初筛推理过程。详细说明：\n1. 分析退回执行快照中的失败步骤和原因\n2. 用户核心需求是什么\n3. 为什么选择这些（或某个）新 Skill\n4. 每个 Skill 解决用户需求的哪一部分（尤其是上次失败的部分）\n5. 如果选择了多个 Skill，说明它们的协作关系\n6. 如果没有任何技能匹配，说明原因和已做的尝试\n7. 如何避免重蹈上次失败的覆辙",
  "no_suitable_skill": "布尔值。\ntrue 表示候选技能中无任何技能匹配用户需求，或根据输入重构判定无需技能。\nfalse 表示至少选中了一个合适的技能。\n当此值为 true 时，selected_skill_ids 必须为 [](空列表)。"
}
```
