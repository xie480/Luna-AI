### 当前系统时间
-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

### 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入输出 JSON 的 `check` 字段。

#### C1：结果筛选校验
1. **有效性过滤**：是否只保留了产生有效结果的技能执行记录？
2. **关键事实提取**：每个技能的 key_facts 是否准确提取了执行结果中的核心信息？

#### C2：去重合并校验
1. **跨轮次去重**：相同技能的多次执行输出是否已正确合并？
2. **信息完整性**：合并后是否保留了各轮次中的关键信息？

请严格按照 system 指令中的 JSON 格式输出，并在 JSON 对象中添加 `check` 字段，不要包含任何额外说明文字。
