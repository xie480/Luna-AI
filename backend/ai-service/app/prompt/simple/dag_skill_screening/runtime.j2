{# Phase 9：Skill 初筛 Prompt — 运行时槽位 #}
## 当前任务阶段
- 职责类型：{{ state_responsibility }}
- 意图：{{ state_intent }}
- 目标：{{ state_goal }}
- 完成标准：{{ completion_criteria }}

-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

## 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

### 2.1 必要性校验
1. **是否真正需要外部技能**：当前 State 的职责类型是什么？纯推理/分析/对话/写作类任务不应选择任何外部技能。
2. **用户意图匹配**：用户请求中是否明确提到了需要使用某种外部能力？还是只是隐含的？

### 2.2 精准性校验
1. **直接关联性**：每个选中的技能是否与当前任务目标**直接相关**？是否存在"可能有用"但不直接相关的技能被选中？
2. **能力覆盖度**：选中的技能组合是否恰好覆盖了任务所需的全部外部能力？有没有遗漏或冗余？

### 2.3 最小化校验
1. **去重检查**：如果多个技能功能重叠，是否只保留了最合适的那一个？
2. **数量合理性**：选中的技能数量是否为满足需求的最小集合？

## 输出要求
请输出 JSON，包含 selected_skills、screening_reason 和 check 字段：
```json
{
  "check": "[必要性] 是否真正需要外部技能：... 用户意图匹配：... [精准性] 直接关联性：... 能力覆盖度：... [最小化] 去重检查：... 数量合理性：...",
  "selected_skills": [
    {
      "skill_name": "技能名称（必须与可用技能列表中的名称完全一致）",
      "relevance_reason": "选择该技能的原因（说明与当前任务目标的直接关联）"
    }
  ],
  "screening_reason": "整体筛选理由"
}
```

**重要提示：**
- 如果当前任务不需要任何外部技能（纯推理/分析/对话/写作任务），请返回空的 `selected_skills` 数组：`{"check":"...", "selected_skills": [], "screening_reason": "当前任务为纯推理任务，无需外部技能"}`。
- 不要因为"可能有用"而选择技能，只在任务目标**明确要求**外部工具时才选择。
- 根据职责类型判断是否需要外部技能：`信息收集`类通常需要搜索工具，`数据分析`/`内容生成`/`总结归纳`类通常不需要。
