{# Phase 9：Skill 初筛 Prompt — 运行时槽位 #}
## 当前任务阶段
- 职责类型：{{ state_responsibility }}
- 意图：{{ state_intent }}
- 目标：{{ state_goal }}
- 完成标准：{{ completion_criteria }}

-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

## 输出要求
请输出 JSON：
```json
{
  "selected_skills": [
    {
      "skill_name": "技能名称（必须与可用技能列表中的名称完全一致）",
      "relevance_reason": "选择该技能的原因（说明与当前任务目标的直接关联）"
    }
  ],
  "screening_reason": "整体筛选理由"
}
```

**重要提示：**
- 如果当前任务不需要任何外部技能（纯推理/分析/对话/写作任务），请返回空的 `selected_skills` 数组：`{"selected_skills": [], "screening_reason": "当前任务为纯推理任务，无需外部技能"}`。
- 不要因为"可能有用"而选择技能，只在任务目标**明确要求**外部工具时才选择。
- 根据职责类型判断是否需要外部技能：`信息收集`类通常需要搜索工具，`数据分析`/`内容生成`/`总结归纳`类通常不需要。
