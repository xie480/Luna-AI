<chat_json_retry_instruction>
## 上一轮输出的格式错误提示
你上一次的输出未通过 JSON 解析器的严格校验。以下是系统捕获的详细错误原因：
<error_detail>
{{ retry_error_info }}
</error_detail>

## 修正与重新输出要求
请深刻反思上述格式错误，并在本次回复中严格执行以下 JSON 格式约束：

1. **唯一合法输出**：你的整个输出必须仅仅是一个合法的单行或多行 JSON 对象，**绝对不能**包含任何 JSON 之外的字符（如 Markdown 代码块标记 ```json、前言、后语等）。
2. **必需字段**：该 JSON 对象必须精确包含以下四个字段（拼写必须完全一致）：
   - `"check"`: 字符串类型，简要记录你在回复前进行的检查和确认。
   - `"thought"`: 字符串类型，记录你生成回复的内部思考过程。
   - `"emotion"`: 字符串类型，表示你当前的情感状态标签（如 "happy", "sad", "neutral" 等）。
   - `"reply"`: 字符串类型，这是你要向用户展示的最终实际回复内容。
3. **格式合法性**：
   - 确保所有的键名（如 `"reply"`）和字符串值都被双引号 `"` 正确包裹。
   - 注意转义字符的使用，特别是当字符串值内部包含双引号或反斜杠时。
   - 确保 JSON 的大括号 `{}` 和逗号 `,` 使用正确且完整。

请基于你原本的回复意图，立即按照上述要求重新生成一次正确的 JSON 输出。
</chat_json_retry_instruction>