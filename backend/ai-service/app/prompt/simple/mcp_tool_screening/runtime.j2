### 当前系统时间
-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

【执行指令】

请根据候选工具列表和用户输入，执行以下步骤：

1. 分析用户输入，拆解需要多少个步骤才能完整满足用户需求。
2. 逐一评估每个候选工具的 core_purpose 和 final_deliverable：
   - 如果单个工具即可满足所有需求 → tool_chain 仅含一个步骤。
   - 如果需要多个工具按顺序协作 → 按执行顺序排列在 tool_chain 中。
     * 例：需要先查城市代码再查天气 → tool_chain=[{tool_name: "CityCodeTool", purpose: "查询城市代码"}, {tool_name: "WeatherTool", purpose: "查询天气"}]
3. 对于 tool_chain 中的每一步，在 purpose 中说明该步骤期望获得什么数据。
4. 如果没有任何工具匹配，将 no_suitable_tool 设为 true，tool_chain 设为空数组。
5. 注意：工具链中的执行顺序必须保证前一个工具的输出是后一个工具的输入依赖。

### 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

#### C1：链路完整性校验
1. **需求覆盖**：tool_chain 是否覆盖了用户完整需求？如果是多步骤任务，每一步是否都有对应工具？
2. **无遗漏**：有没有遗漏了必要的中间步骤？（如查天气前需要先查城市代码）

#### C2：顺序合理性校验
1. **依赖保证**：执行顺序是否保证了前一个工具的输出是后一个工具的输入依赖？
2. **最小化**：tool_chain 的长度是否为满足需求的最小步骤数？

#### C3：工具合法性校验
1. **来源合法**：所有 tool_name 是否都来自候选工具列表？

必须严格输出以下 JSON 结构：

```json
{
  "check": "[链路完整性] 需求覆盖：... 无遗漏：... [顺序合理性] 依赖保证：... 最小化：... [工具合法性] 来源合法：...",
  "tool_chain": [
    {
      "tool_name": "要调用的工具名称，必须来自候选工具列表",
      "purpose": "调用该工具的目的，说明期望从该工具获得什么数据"
    }
  ],
  "reasoning": "初筛推理过程，解释为什么选择这些工具以及为什么按此顺序排列",
  "no_suitable_tool": "布尔值，标记为 true 表示无任何工具能匹配用户需求，此时 tool_chain 应为空数组"
}
```
