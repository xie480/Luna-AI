{# Phase 9：全局 Plan 生成 Prompt — 运行时槽位 #}
## 用户输入
{{ disambiguated_text }}

{% if unresolved_pronouns %}
## 注意：以下代词未能消歧
{% for p in unresolved_pronouns %}
- {{ p.original }}: {{ p.reason }}
{% endfor %}
请在规划时考虑这些信息缺失，必要时先搜索确认。
{% endif %}

-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

## 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

### 2.1 需求理解校验
1. **意图完整性**：是否完整理解了用户请求的所有子目标？有没有遗漏隐含需求？
2. **复杂度判断**：用户请求是否真的需要多个 State？如果只涉及单一职责，应只生成 1 个 State。

### 2.2 职责拆分校验
1. **单一职责原则**：每个 State 是否只承担一种明确的职责类型？有没有将"搜索"和"分析"混在同一个 State 中？
2. **禁止按难易度拆分**：是否存在按工作量而非职责类型拆分的 State？（如"简单搜索"和"复杂分析"应改为"信息收集"和"数据分析"）
3. **禁止职责重叠**：是否存在两个 State 承担相同类型职责的情况？

### 2.3 依赖与数据流校验
1. **依赖正确性**：每个 State 的 `depends_on` 是否反映了真实的数据依赖？下游 State 是否确实需要上游 State 的产出？
2. **无循环依赖**：是否存在循环依赖或无效依赖？

### 2.4 技能选择校验
1. **必要性**：`required_skill_names` 中的每个技能是否确实被该 State 需要？纯推理/写作任务是否错误地选择了外部技能？
2. **最小化**：是否只选择了必要的技能，而非"可能有用"的技能？

### 2.5 全局覆盖校验
1. **完整性**：所有 State 组合是否覆盖了用户请求的全部意图？
2. **边界清晰**：State 之间的职责边界是否清晰无歧义？

## 输出要求
请输出 JSON，包含 states、planning_reason 和 check 字段：
```json
{
  "check": "[需求理解] 意图完整性：... 复杂度判断：... [职责拆分] 单一职责：... 禁止按难易度拆分：... 禁止职责重叠：... [依赖与数据流] 依赖正确性：... 无循环依赖：... [技能选择] 必要性：... 最小化：... [全局覆盖] 完整性：... 边界清晰：...",
  "states": [
    {
      "order_index": 0,
      "responsibility": "职责类型（如：信息收集、数据分析、内容生成、验证校对等）",
      "intent": "[职责类型]: 具体意图描述（简短）",
      "goal": "该阶段期望达成的具体目标（详细描述，明确输入来源和输出产物）",
      "completion_criteria": [
        {"field": "字段名", "operator": ">=", "value": 1}
      ],
      "depends_on": [],
      "required_skill_names": []
    }
  ],
  "planning_reason": "为什么这样按职责拆分 State 的理由，说明每个 State 职责边界"
}
```

### 字段说明
- **responsibility**（必填）：该 State 承担的**唯一职责类型**。必须是单一职责，禁止将多种职责混在一个 State 中。常见值：信息收集、数据分析、内容生成、知识检索、格式转换、验证校对、总结归纳、方案设计、代码实现、测试验证。
- **intent**（必填）：格式为 `[职责类型]: 具体意图描述`，与 responsibility 保持一致。
- **goal**（必填）：详细描述该 State 的目标，必须说明输入来源（依赖哪些上游 State 的产出）和期望的输出产物。
- **depends_on**：填写依赖的 State 的 `order_index`（整数）。反映数据流向，而非先后顺序。
- **required_skill_names**（可选）：只有当该 State 明确需要调用外部工具或加载技能资源时才填写；纯推理/对话/写作类任务应留空数组 `[]`。

### 拆分自检清单
在输出前，请逐条检查：
1. ✅ 每个 State 是否只承担一种职责？（如果一个 State 既要搜索又要分析，必须拆开）
2. ✅ 是否存在职责重叠的 State？（两个 State 做同一件事，应合并）
3. ✅ State 数量是否合理？（职责单一的前提下，通常 2-5 个 State）
4. ✅ depends_on 是否反映了真实的数据依赖？（下游 State 确实需要上游的产出）
5. ✅ 简单请求是否只生成了 1 个 State？（不要过度拆分简单任务）
