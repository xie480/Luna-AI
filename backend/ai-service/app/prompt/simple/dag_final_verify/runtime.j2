{# Phase 9：最终验收 Prompt — 运行时槽位 #}
## 全局目标

**目标**：{{ global_goal }}
**详细描述**：{{ goal_definition }}
**验收标准**：{{ acceptance_criteria }}
**非目标**：{{ non_goals }}

## 步骤执行结果

{{ step_results }}

## 执行统计

**计划版本**：{{ plan_version }}
**重规划次数**：{{ replan_count }}
**工具调用次数**：{{ tool_calls_used }}
**是否终止**：{{ terminated }}
{% if termination_reason %}
**终止原因**：{{ termination_reason }}
{% endif %}

-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

## 输出要求
请输出 JSON，包含 status、report、all_criteria_met 和 criteria_verification 字段：
{
  "status": "验收状态，二选一：pass（通过）/ fail（失败）",
  "report": "直接面向用户的详细答复内容。请基于步骤执行结果，直接输出详尽的分析、具体事实、收集到的数据以及最终结论本身。严禁使用“我生成了一份报告”、“我成功完成了任务”这类概括性流水账语言。",
  "all_criteria_met": true,
  "criteria_verification": [
    {
      "criterion": "验收标准条目原文",
      "met": true,
      "evidence": "满足/未满足的具体依据，引用步骤执行结果中的事实"
    }
  ]
}
status 的判断依据：所有验收标准均满足选 pass；关键标准未满足选 fail。
all_criteria_met 为 true 当且仅当 criteria_verification 中所有条目 met 均为 true。
