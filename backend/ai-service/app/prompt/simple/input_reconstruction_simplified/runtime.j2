{# 简化输入重构 — 运行时槽位 #}
## 用户原始输入
{{ CURRENT_MESSAGE }}

-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

## 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

### 2.1 消歧质量校验
1. **代词识别完整性**：是否识别了用户输入中所有的代词和指代词？
2. **替换准确性**：已消歧的代词是否替换了正确的实体？
3. **边界约束**：是否严格只做了代词消歧，没有做任何路由决策、意图分类或内容润色？

## 输出要求
请输出 JSON，包含 disambiguated_text、unresolved_pronouns 和 check 字段：
{
  "check": "[消歧质量] 代词识别完整性：... 替换准确性：... 边界约束：...",
  "disambiguated_text": "消歧后的完整文本。将你能找到的代词替换为具体实体，找不到的用 [未知实体] 标记。",
  "unresolved_pronouns": [
    {
      "original": "无法消歧的代词原文",
      "reason": "无法消歧的原因"
    }
  ]
}
只做代词消歧，不做任何路由决策、意图分类或内容润色。
