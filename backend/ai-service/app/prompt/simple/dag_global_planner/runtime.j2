{# Phase 9：全局计划生成 Prompt — 运行时槽位 #}
## 已锁定的全局目标

**目标**：{{ global_goal }}
**详细描述**：{{ goal_definition }}
**验收标准**：{{ acceptance_criteria }}
**非目标声明**：{{ non_goals }}
**约束条件**：{{ constraints }}

## 用户输入（已消歧）

{{ disambiguated_text }}

-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

## 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

### 2.1 需求理解校验
1. **意图完整性**：是否完整理解了用户请求的所有子目标？有没有遗漏隐含需求？
2. **复杂度判断**：用户请求是否真的需要多个 State？如果只涉及单一职责，应只生成 1 个 State。

### 2.2 职责拆分校验
1. **单一职责原则**：每个 State 是否只承担一种明确的职责类型？有没有将"搜索"和"分析"混在同一个 State 中？
2. **禁止过度拆分**：是否存在将同一职责的子操作拆成多个 State 的情况？（如"搜索A数据"和"搜索B数据"应合并为一个"信息收集" State）
3. **禁止职责重叠**：是否存在两个 State 承担相同类型职责的情况？

### 2.3 依赖与数据流校验
1. **依赖正确性**：每个 State 的 `dependencies` 是否反映了真实的数据依赖？下游 State 是否确实需要上游 State 的产出？
2. **无循环依赖**：是否存在循环依赖或无效依赖？

### 2.4 技能选择校验
1. **必要性**：`pre_allocated_skills` 中的每个技能是否确实被该 State 需要？纯推理/写作任务是否错误地选择了外部技能？
2. **最小化**：是否只选择了必要的技能，而非"可能有用"的技能？

### 2.5 全局覆盖校验
1. **完整性**：所有 State 组合是否覆盖了用户请求的全部意图？
2. **边界清晰**：State 之间的职责边界是否清晰无歧义？

## 输出要求
请输出 JSON，包含 states、planning_reason 和 check 字段：
{
  "check": "[需求理解] 意图完整性：... 复杂度判断：... [职责拆分] 单一职责：... 禁止过度拆分：... 禁止职责重叠：... [依赖与数据流] 依赖正确性：... 无循环依赖：... [技能选择] 必要性：... 最小化：... [全局覆盖] 完整性：... 边界清晰：...",
  "states": [
    {
      "responsibility": "职责类型（如：信息收集、数据分析、内容生成、验证校对等）",
      "intent": "[职责类型]: 具体意图描述",
      "goal": "该阶段期望达成的具体目标（详细描述，明确输入来源和输出产物）",
      "expected_output": "预期产出物描述",
      "completion_criteria": [
        {"field": "字段名", "operator": ">=", "value": 1}
      ],
      "dependencies": [],
      "risk_notes": "风险点：该阶段可能遇到的困难或失败场景",
      "rollback_hint": "回退提示：如果该阶段失败，应该如何回退或降级处理",
      "pre_allocated_skills": [
        {"skill_name": "技能名称", "relevance_reason": "选择理由"}
      ]
    }
  ],
  "planning_reason": "整体规划理由，说明为什么按此方式拆分职责、各 State 之间的逻辑关系"
}

### 字段说明
- **responsibility**（必填）：该 State 承担的**唯一职责类型**。必须是单一职责，禁止将多种职责混在一个 State 中。常见值：信息收集、数据分析、内容生成、知识检索、格式转换、验证校对、总结归纳、方案设计、代码实现、测试验证。
- **intent**（必填）：格式为 `[职责类型]: 具体意图描述`，与 responsibility 保持一致。
- **goal**（必填）：详细描述该 State 的目标，必须说明输入来源（依赖哪些上游 State 的产出）和期望的输出产物。
- **expected_output**（必填）：预期产出物描述，用于后续评估该 State 是否完成。
- **dependencies**（必填）：填写依赖的前置 State 的索引（整数数组，如 `[0]`）。反映数据流向——只有当前置 State 完成并产出了必要数据，当前 State 才能开始。
- **pre_allocated_skills**（可选）：预分配技能列表。每个对象必须包含 `skill_name`（必须与“可用能力参考”中的名称一致）和 `relevance_reason`。只有当该 State 明确需要调用外部工具或加载技能资源时才填写；纯推理/对话/写作类任务应留空数组 `[]`。

### 拆分自检清单
在输出前，请逐条检查：
1. ✅ 每个 State 是否只承担一种职责？（如果一个 State 既要搜索又要分析，必须拆开）
2. ✅ 是否存在同一职责被拆成多个 State？（如"搜索A数据"和"搜索B数据"应合并）
3. ✅ State 数量是否在 2~5 个之间？（职责单一的前提下，不要过度拆分）
4. ✅ dependencies 是否反映了真实的数据依赖？（下游 State 确实需要上游的产出）
5. ✅ 简单请求是否只生成了 1 个 State？（不要过度拆分简单任务）
