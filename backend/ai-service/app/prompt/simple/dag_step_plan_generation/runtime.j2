{# Phase 9：Step Plan 生成 Prompt — 运行时槽位 #}
## 当前任务阶段
- 职责类型：{{ state_responsibility }}
- 意图：{{ state_intent }}
- 目标：{{ state_goal }}

-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

## 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

### 2.1 节点类型校验
1. **类型匹配**：每个 node 的 `node_type` 是否正确匹配了其职责？需要外部数据的是否用了 `tool_execute`？需要加载资源的是否用了 `resource_loading`？纯推理/转换的是否用了 `data_transform`？
2. **无技能时的降级**：如果已筛选技能列表为空，是否优先使用 `data_transform` 节点？有没有伪造 skill_name 或 tool_name？

### 2.2 依赖关系校验
1. **同 Step 内依赖**：`depends_on` 是否只引用了同一 Step 内的节点索引？有没有跨 Step 引用？
2. **数据流完整性**：需要前序节点输出作为输入的节点，是否正确声明了 `depends_on`？
3. **无遗漏依赖**：有没有节点实际需要前序数据但未声明依赖的情况？

### 2.3 并行合理性校验
1. **并行机会利用**：无依赖关系的节点是否被放在了同一 Step 中？有没有不必要的串行化？
2. **Step 粒度**：Step 数量是否为最小必要数量？

### 2.4 参数具体性校验
1. **parameter_hint 质量**：工具执行节点的 `parameter_hint` 是否足够具体？是否说明了参数含义、取值范围和格式要求？
2. **transform_instruction 质量**：数据转换节点的 `transform_instruction` 是否明确了输入来源和期望输出格式？

## 输出要求
请输出 JSON，结构如下。每个 Step 包含 description 和 nodes 数组，每个 node 的字段根据 node_type 的不同而有所不同。同时必须包含 `check` 字段：

```json
{
  "check": "[节点类型] 类型匹配：... 无技能降级：... [依赖关系] 同 Step 内依赖：... 数据流完整性：... 无遗漏依赖：... [并行合理性] 并行机会利用：... Step 粒度：... [参数具体性] parameter_hint 质量：... transform_instruction 质量：...",
  "steps": [
    {
      "description": "步骤描述（简要说明这个步骤做什么）",
      "nodes": [
        {
          "node_type": "resource_loading | tool_execute | data_transform",

          "skill_name": "所属技能名称（resource_loading 和 tool_execute 时必填，填写已筛选技能中的 skill_name）",

          "tool_name": "工具名称（tool_execute 时必填，填写已筛选技能工具列表中的工具 name）",

          "resource_name": "资源名称（resource_loading 时必填，填写已筛选技能资源列表中的资源 name）",

          "parameter_hint": "参数提取提示（tool_execute 时必填，描述如何从上下文中提取工具参数，包括参数含义、取值范围、格式要求等）",

          "transform_instruction": "转换指令（data_transform 时必填，详细描述需要做什么数据转换，如：从搜索结果中提取表格数据、将 JSON 转为 Markdown 等）",

          "depends_on": [0, 1],

          "gating_required": false
        }
      ]
    }
  ]
}
```

### 字段说明
- **check**（必填）：生成前推演校验结果。按 [节点类型][依赖关系][并行合理性][参数具体性] 四个维度逐一校验并记录，用于 CoT 推理保证输出质量。
- **depends_on**：整数数组，填写同一 Step 内依赖节点的索引。例如 `[0, 1]` 表示依赖当前 Step 中第 0 和第 1 个节点的输出。跨 Step 的数据自动可见，无需声明。
- **gating_required**：布尔值，当工具风险等级为 L2/L3 时设为 true，表示需要用户审批后才能执行。默认为 false。

### 注意事项
1. 同一 Step 内的节点可以并行执行，不同 Step 按顺序执行。
2. depends_on 只引用同一 Step 内的节点索引，不要跨 Step 引用。
3. 每种 node_type 只填写其必填字段，其他字段留空或省略。
4. parameter_hint 和 transform_instruction 要具体明确，让执行器能准确理解意图。
5. 合理利用并行：将无依赖关系的节点放在同一 Step 中以提高效率。
6. **无技能时的处理**：如果已筛选技能列表为空，请优先使用 `data_transform` 节点直接完成任务（如纯推理、写作、分析）。不要伪造 skill_name 或 tool_name。
