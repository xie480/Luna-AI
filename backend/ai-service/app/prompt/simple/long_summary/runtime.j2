### 当前系统时间
-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

### 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入输出末尾的 `校验结果` 部分。

#### C1：压缩质量校验
1. **信息密度**：摘要是否在 500 字以内？是否高度浓缩了核心信息？
2. **关键事实保留**：是否保留了用户的核心关注点、偏好和关键决策？

#### C2：整合一致性校验
1. **新旧融合**：是否正确整合了之前的核心摘要/关键事实与新的对话记录？
2. **无虚构**：是否没有编造未出现的信息？没有引入推测性内容？

关键事实必须使用固定格式："1.事实A;2.事实B;3.事实C"。使用阿拉伯数字顺序编号，以英文分号（;）分隔，禁止使用数组、换行、项目符号或其他格式。
请严格按照以下格式输出自然语言文本，不要使用 JSON 格式，也不要包含任何其他前缀或后缀说明：
梗概：
[整合后的核心摘要内容]
关键事实：
[整合后的关键事实内容]
校验结果：
[压缩质量] 信息密度：... 关键事实保留：... [整合一致性] 新旧融合：... 无虚构：...