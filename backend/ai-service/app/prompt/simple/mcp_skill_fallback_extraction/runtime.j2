### 当前系统时间
-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

### 执行快照生成详细指南

请严格按照以下步骤和方法生成执行快照：

#### Step 1：理解原执行计划
- 解析【原执行计划全貌】，了解本次执行原本规划了哪些步骤。
- 每个步骤对应一个 `state_key`，包含关联的 `skill`、`resource`、`tool`、`goal` 信息。

#### Step 2：匹配执行结果
对原执行计划中的每个步骤，匹配对应的执行结果：

1. **资源加载步骤**：匹配【资源加载结果】中对应 `resource_name` 的条目。
   - `status`：根据 `success` 字段设为"已加载"或"加载失败"。
   - `result`：如果成功，填入 `extracted_info` 的前 500 字符摘要；如果失败，填入 `error_message`。

2. **工具执行步骤**：匹配【工具执行结果】中对应 `tool_name` 的条目。
   - `status`：根据 `success` 字段设为"已执行"或"执行失败"。
   - `result`：如果成功，填入 `output_text` 的前 500 字符摘要；如果失败，填入 `error_message`。

3. **未执行步骤**：如果某个步骤在执行结果中找不到对应数据，`status` 设为"未执行"，`result` 设为空字符串。

#### Step 3：保留计划结构
- 输出结构必须保留原执行计划中的 `state_key` 和关联的 `skill`/`resource`/`tool`/`goal` 信息。
- 仅新增 `status` 和 `result` 字段。

#### Step 4：信息压缩
- 如果某个 `result` 过长（超过 500 字符），取前 500 字符并追加 `…（截断）` 标记。
- 保持 JSON 结构简洁紧凑。

---

### ⚠️ 严格输出格式要求 ⚠️

你必须严格输出以下 JSON 结构，必须合法。
【致命约束】：`state_key` 必须严格来自原执行计划，**禁止虚构或重命名**！
输出必须是一个合法的 JSON 对象，**禁止**包含任何额外的解释性文本、Markdown 代码块标记或注释。

```json
{
  "state1": {
    "skill": "此步骤关联的技能名称。来自原执行计划。",
    "resource": "此步骤加载的资源名称。来自原执行计划。",
    "tool": "此步骤使用的工具名称。来自原执行计划。",
    "goal": "此步骤的执行目标。来自原执行计划。",
    "status": "执行状态：已加载 / 加载失败 / 已执行 / 执行失败 / 未执行",
    "result": "执行结果摘要。成功时填充输出的前 500 字符；失败时填充错误描述。未执行时为空字符串。"
  },
  "state2": {
    "skill": "",
    "resource": "",
    "tool": "",
    "goal": "",
    "status": "",
    "result": ""
  }
}
```
