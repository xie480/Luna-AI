### 当前系统时间
-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

### 记忆变量提取详细指南

请严格按照以下步骤和方法从历史执行日志中提取记忆变量：

#### Step 1：梳理执行历程
- 从【历史执行日志】中还原每一轮的执行上下文：包括每一轮使用的工具、参数、输入和输出。
- 梳理每一轮返回了什么结果？哪些返回了实质性内容？哪些结果为空或报错？
- 记录哪些参数组合产生了有效结果，哪些被证明是无效尝试。

#### Step 2：分析进展与缺口
- 对照用户的最终目标意图，标记已获得的关键信息。
- 判断以下问题的答案状态：
  - 用户的核心问题是否已有足够的证据支撑？
  - 如果信息还不完整，最核心的缺口是什么？
  - 历史中有哪些尝试被证明是无效的（需要避开）？

#### Step 3：制定下一轮策略
- 基于缺口分析，决策是否需要继续当前工具的执行。
- 如果需要继续，给出本轮建议的执行参数调整方向（如查询词、筛选条件、范围限定等）。
- 策略思考要点：
  - 历史尝试过的无效参数组合不要再重复。
  - 根据之前的执行结果，思考是否应调整入参的关键字段。

#### Step 4：填充所有变量
- 输出 Schema 中声明的所有字段，不要遗漏。
- 对于无记录的字段，用空值或默认值填充以保证 JSON 结构完整。

---

### 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

#### C1：历史梳理校验
1. **完整还原**：是否从历史执行日志中完整还原了每一轮的执行上下文（工具、参数、输入、输出）？
2. **有效/无效区分**：是否正确区分了产生实质性内容的结果和为空/报错的结果？

#### C2：策略有效性校验
1. **缺口识别**：是否准确识别了当前信息的核心缺口？
2. **避免重复**：下一轮策略是否避免了历史中被证明无效的参数组合？
3. **调整方向**：参数调整方向是否基于历史数据的合理推断？

#### C3：变量完整性校验
1. **Schema 遵从**：输出是否严格符合 MEMORY_SCHEMA 的结构定义？
2. **无遗漏字段**：所有声明的变量是否都已填充？无记录的字段是否用了默认值？

### ⚠️ 严格枚举约束与输出格式要求 ⚠️

你必须严格输出符合当前技能所声明的 JSON Schema 的结构，并在末尾附加 `check` 字段。Schema 定义如下：
{{ MEMORY_SCHEMA | default("（当前技能未声明特定的记忆 Schema，请根据通用上下文推断输出结构）") }}

请在 JSON 输出的最外层添加 `check` 字段，值为校验结果字符串。

【致命约束】：所有策略决策必须基于真实的历史执行数据，禁止编造不存在的执行结果。
输出必须是一个合法的 JSON 对象，**禁止**包含任何额外的解释性文本、Markdown 代码块标记或注释。
