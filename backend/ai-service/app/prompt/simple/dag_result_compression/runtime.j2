{# Phase 9：结果压缩 Prompt — 运行时槽位 #}
## 任务目标
{{ state_goal }}

## 执行结果
成功节点：{{ nodes_succeeded }}，失败节点：{{ nodes_failed }}

### 合并输出
{{ merged_output }}

### 错误信息
{{ error_messages }}

## 评估不通过原因
{{ evaluation_reason }}

## 差距分析
{{ gap_analysis }}

-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

## 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

### 2.1 压缩质量校验
1. **信息密度**：compressed_summary 是否在 200 字以内？是否高度浓缩了关键信息？
2. **关键事实保留**：key_facts 是否提取了对 Plan 重构最有价值的事实？有没有遗漏关键信息？

### 2.2 重构价值校验
1. **可操作性**：压缩后的信息是否足以支撑 Plan 重构 Agent 做出正确的调整决策？
2. **错误归因**：error_summary 是否准确归因了失败原因，而非笼统描述？

## 输出要求
请输出 JSON，将以上信息压缩为精简摘要，同时包含 check 字段：
{
  "check": "[压缩质量] 信息密度：... 关键事实保留：... [重构价值] 可操作性：... 错误归因：...",
  "compressed_summary": "200字以内的精简摘要，保留关键事实、已获取的资源和错误原因",
  "key_facts": ["关键事实1", "关键事实2"],
  "error_summary": "错误原因的简要总结",
  "resource_summary": "已获取资源的简要总结"
}
只保留对 Plan 重构有参考价值的信息，丢弃冗余细节。
