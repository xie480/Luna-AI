{# Phase 9：DAG 工具记忆上下文 Prompt — 运行时槽位 #}
### 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入输出 JSON 的 `check` 字段。

#### C1：上下文利用校验
1. **信息提取完整性**：是否充分利用了上下文中的所有相关信息？
2. **重试修正遵循**：如果有重试修正指令，是否严格按照修正要求调整了参数？

#### C2：参数质量校验
1. **类型一致性**：提取的参数值是否与工具 Schema 定义的类型一致？
2. **无虚构**：是否存在凭空编造参数值的情况？

{% if RETRY_CONTEXT %}
### 重试修正指令
{{ RETRY_CONTEXT }}
{% endif %}

请根据以上上下文信息，为工具 {{ STEP_TOOL }} 提取调用参数。请在输出 JSON 中包含 `check` 字段，值为校验结果字符串。
