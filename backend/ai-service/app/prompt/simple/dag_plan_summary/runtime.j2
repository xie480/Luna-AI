{# Phase 9：Plan 汇总 Prompt — 运行时槽位 #}
## 执行概况
- 总阶段数：{{ total_states }}
{% if terminated %}
- 状态：异常终止
- 终止原因：{{ termination_reason }}
{% else %}
- 状态：全部完成
{% endif %}

## 各阶段执行结果
{{ state_summaries }}

-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

## 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

### 2.1 信息完整性校验
1. **全量覆盖**：是否覆盖了所有 State 的执行结果？有没有遗漏某个 State 的成败情况？
2. **关键事实保留**：是否保留了每个 State 中最重要的执行成果和关键数据？

### 2.2 面向用户表达校验
1. **语言自然性**：overall_result 是否为自然流畅的描述？有没有使用技术术语或状态码？
2. **信息层次**：最重要的信息是否放在了最前面？用户能否快速了解执行结果？

### 2.3 客观性校验
1. **无虚构**：是否如实反映了执行结果？没有美化失败、没有夸大成功？
2. **问题透明**：execution_issues 是否准确反映了实际遇到的问题？

## 输出要求
请输出 JSON，包含 overall_result、execution_highlights、execution_issues 和 check 字段：
{
  "check": "[信息完整性] 全量覆盖：... 关键事实保留：... [面向用户表达] 语言自然性：... 信息层次：... [客观性] 无虚构：... 问题透明：...",
  "overall_result": "面向用户的执行结果汇总文本，说明完成了什么、哪些成功、哪些失败，语言自然流畅",
  "execution_highlights": ["亮点1：具体完成的内容", "亮点2"],
  "execution_issues": ["问题1：具体遇到的问题", "问题2"]
}
overall_result 应该是一段完整的、面向用户的自然语言描述，不要使用 JSON 结构。
