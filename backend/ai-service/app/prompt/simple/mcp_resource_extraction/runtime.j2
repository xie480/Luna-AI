### 当前系统时间
-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

### 语义提取详细指南

请严格按照以下步骤和方法执行资源信息提取：

#### Step 1：理解加载目的
- 理解本次加载目的：**{{ LOAD_PURPOSE }}**
- 明确你需要从文件内容中寻找什么类型的信息（代码段、配置项、数据定义、注释说明等）。
- 如果加载目的模糊，优先提取文件中定义最清晰、结构最完整的语义单元。

#### Step 2：逐区域语义扫描
- 将文件内容按逻辑块（函数定义、类定义、配置块、数据段、注释段落）进行划分。
- 对每个逻辑块评估其与加载目的的语义相关性，分为三个等级：
  - **高相关**：直接实现加载目的的核心逻辑。
  - **中相关**：辅助性代码、依赖定义、配置引用。
  - **低相关**：无关的工具函数、调试代码、废弃注释。
- 只提取 **高相关** 和 **中相关** 的语义单元。

#### Step 3：语义单元保留规则
- 提取时 **必须保留完整语义单元**（完整函数、完整类、完整配置块），禁止在代码块中间截断。
- 如果某个逻辑块超过 200 行，可以只提取其签名、核心逻辑段和关键注释，但必须在 `summary` 中说明省略部分的作用。
- 对每个提取的片段标注其在文件中的 **起始行号和结束行号**（基于文件中每行已标注的括号行号）。

#### Step 4：相关性判定边界
- **部分相关**：如果文件内容与加载目的部分相关，提取最相关的部分，在 `summary` 中标注相关程度，忽略不相关的内容。
- **完全无关**：如果文件内容与加载目的完全无关，将 `has_relevant_info` 设为 `false`，`extractions` 设为空数组，并在第一个 `summary` 中注明"文件中未找到与加载目的相关的信息"。
- **文件为空**：如果文件内容为空，同样 `has_relevant_info=false`，并在 `summary` 中注明。

#### Step 5：行号校验
- 所有输出的行号必须位于文件的有效行号范围内（1 到总行数之间）。
- 行号范围应为闭区间 `[start_line, end_line]`，确保覆盖完整的语义单元。

---

### ⚠️ 严格输出格式要求 ⚠️

你必须严格输出以下 JSON 结构，**禁止**包含任何额外的解释性文本、Markdown 代码块标记或注释。

```json
{
  "has_relevant_info": "布尔值。true 表示文件中存在与加载目的相关的有用信息；false 表示文件中未找到相关信息或文件为空。",
  "extractions": [
    {
      "extracted_content": "必须提取原文中有价值的关键段落，进行深度概括与重组，融合成一段通顺、连贯且逻辑清晰的自然语言文本。\n如果 has_relevant_info=false，此数组为空。",
      "matched_lines": [
        起始行号（整数，1-based，表示提取内容在文件中的起始行号）,
        结束行号（整数，1-based，表示提取内容在文件中的结束行号）
      ],
      "summary": "对提取内容的简要说明，必须准确对应并附上生成上述概括内容所参考的原始文本的具体行号。包括：提取了什么类型的语义单元、与加载目的的关联程度、\n是否完整提取了该语义单元（如有省略需说明原因）。\n如果 has_relevant_info=false，注明'文件中未找到与加载目的相关的信息'。"
    }
  ]
}
```
