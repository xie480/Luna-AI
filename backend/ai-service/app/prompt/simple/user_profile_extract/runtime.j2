# 用户画像提取 - 运行时输入

当前时间：{{CURRENT_TIME}}
来源会话 ID：{{SESSION_ID}}

## 待分析聊天记录
{{MESSAGES_TEXT}}

## 输出 JSON Schema
{{OUTPUT_SCHEMA}}

请只返回符合 Schema 的 JSON。若没有可信画像候选，返回空 candidates，并把被拒绝的可疑内容放入 rejected_candidates。
