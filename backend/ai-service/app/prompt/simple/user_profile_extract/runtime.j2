# 用户画像提取 - 运行时输入

当前时间：{{ CURRENT_TIME }}
来源会话 ID：{{ SESSION_ID }}

## 待分析聊天记录
{{ MESSAGES_TEXT }}

## 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

#### C1：证据质量校验
1. **原始证据**：每个 mutation 的 evidence 是否摘自聊天记录的原始片段？有没有编造？
2. **风险标记**：source_risk_flags 是否准确标记了假设、玩笑、反讽等不可靠来源？

#### C2：动作合理性校验
1. **action 准确性**：add/confirm_existing/supersede/reject 的选择是否合理？
2. **新旧区分**：是否正确区分了新画像（add）和与已有画像重复的内容（confirm_existing/supersede）？

#### C3：画像质量校验
1. **稳定性**：提取的画像是否为关于用户的稳定特征，而非临时状态？
2. **confidence 合理性**：置信度是否基于证据质量合理赋值？

## 输出 JSON Schema
{
  "check": "[证据质量] 原始证据：... 风险标记：... [动作合理性] action准确性：... 新旧区分：... [画像质量] 稳定性：... confidence合理性：...",
  "schema_version": "user_profile.mutation.v1",
  "mutations": [
    {
      "action": "add | confirm_existing | supersede | reject",
      "target_item_id": "当 action 为 confirm_existing 或 supersede 时必填，否则为空",
      "reason": "必须为中文，说明为什么这么处理",
      "candidate": {
        "category": "appearance | personality | likes | dislikes | fears | expectations | habits | custom",
        "custom_category_name": "category=custom 时必填，否则为 null",
        "content": "关于用户本人的稳定画像正文，长度 4 到 200 字",
        "evidence": "必须摘自聊天记录的原始证据片段，长度 4 到 300 字",
        "confidence": 0.0,
        "source_risk_flags": ["hypothetical | joke | sarcasm | perfunctory | quote | roleplay | temporary_emotion | fictional"],
        "reasoning": "必须为中文，说明为什么判断为该画像"
      }
    }
  ]
}

## 输出要求
1. 如果没有可信画像，返回空 mutations。
2. 对于假设、玩笑、反讽、敷衍、引用、角色扮演、临时情绪、虚构设定，应输出 action=reject。
3. 对于与已有画像重复的内容，应优先输出 action=confirm_existing。
4. 对于明确替换旧画像的新事实，应输出 action=supersede，并填写 target_item_id。
5. 不要输出任何 JSON 之外的内容。
