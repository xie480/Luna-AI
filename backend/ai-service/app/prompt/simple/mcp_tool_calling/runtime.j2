### 当前系统时间
-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

<strong>【参数提取指令】</strong>

1. 仔细阅读工具的 parameters_schema，逐字段对照用户输入。
2. 对于每个参数：
   a. 优先从用户输入的明确表述中提取。
   b. 如果用户输入中指代不明，结合 MEMORY_SNIPPETS 中的上下文记忆推断。
   c. {% if PREVIOUS_TOOL_RESULT %}如果仍不明确，检查 PREVIOUS_TOOL_RESULT 中是否包含所需数据。{% endif %}
   d. 如果仍无法确定，对于可选参数留空，对于必填参数标记失败。
3. 枚举类型参数必须在 enum 列表中取值，禁止编造。
4. 所有参数值必须与 Schema 中定义的类型一致。
5. 在 parameter_explanation 中简要说明每个关键参数的提取依据。

### 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

#### C1：Schema 对齐校验
1. **类型一致性**：每个参数值是否严格遵循 parameters_schema 中定义的类型？
2. **枚举约束**：枚举类型参数是否在 enum 列表中取值？有没有编造值？

#### C2：参数来源校验
1. **优先级遵循**：是否按优先级顺序提取参数（用户输入 > 上下文记忆 > 前序结果）？
2. **必填完整性**：所有必填参数是否都已提取？有没有遗漏？

#### C3：失败判定校验
1. **合理性**：call_parameters_failed 的判定是否合理？是真的无法提取还是可以推断？
2. **原因明确**：failure_reason 是否清楚说明了失败的具体原因？

必须严格输出以下 JSON 结构：

```json
{
  "check": "[Schema对齐] 类型一致性：... 枚举约束：... [参数来源] 优先级遵循：... 必填完整性：... [失败判定] 合理性：... 原因明确：...",
  "parameters": "调用当前工具所需的最终参数键值对，严格遵循当前工具的 parameters_schema",
  "parameter_explanation": "参数提取过程的说明，解释每个参数值是如何从用户输入、上下文或前序结果中提取的",
  "call_parameters_failed": "布尔值，标记为 true 表示当前工具的参数提取失败，上层应终止工具链",
  "failure_reason": "参数提取失败的具体原因，call_parameters_failed=true 时必须填写"
}
```
