{# Phase 9：数据转换 Prompt — 运行时槽位 #}
## 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

### 2.1 指令遵循校验
1. **完整性**：是否严格按照了转换指令的所有要求进行处理？有没有遗漏指令中的某个子步骤？
2. **准确性**：转换逻辑是否正确？有没有误解指令的意图？

### 2.2 信息保真校验
1. **无丢失**：转换过程中是否丢失了输入数据中的关键信息？
2. **无虚构**：输出中是否包含了输入数据中不存在的信息？

## 输入数据
{{ input_data }}

## 转换指令
{{ transform_instruction }}

## 输出要求
请输出单行 JSON，`check` 为第一个字段，`result` 为转换后的结果：
```json
{"check":"[指令遵循] 完整性：... 准确性：... [信息保真] 无丢失：... 无虚构：...","result":"转换后的输出结果"}
```
