{# Phase 9：Plan 重构 Prompt — 运行时槽位 #}
## 失败的阶段
- ID: {{ failed_state_id }}
- 职责: {{ failed_state_responsibility }}
- 意图: {{ failed_state_intent }}
- 目标: {{ failed_state_goal }}

## 失败阶段的执行结果（压缩后）
{{ failed_state_result }}

## 评估不通过原因
{{ evaluation_reason }}

## 差距分析
{{ gap_analysis }}

## 改进建议
{{ suggestion }}

## 待修改的后续阶段
{{ remaining_states }}

## 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

### 2.1 失败根因校验
1. **根因定位**：是否准确识别了失败阶段的核心问题？是目标设定不合理、技能选择不当、还是执行策略有误？
2. **不可变约束**：是否严格遵守了"不修改已完成阶段"的约束？

### 2.2 调整合理性校验
1. **职责单一性**：调整后的每个 State 是否仍然只承担单一职责？有没有因调整而引入职责混合？
2. **职责完整性**：调整后的 State 组合是否仍能覆盖达成全局目标所需的全部职责？
3. **目标一致性**：调整后的计划是否仍能达成全局目标？有没有因过度简化而丢失必要步骤？

### 2.3 技能选择校验
1. **必要性**：调整后的 `required_skill_names` 是否确实被需要？有没有因过度补偿而强选技能？

## 输出要求
请输出 JSON，包含 revised_states、replan_reason 和 check 字段：
```json
{
  "check": "[失败根因] 根因定位：... 不可变约束：... [调整合理性] 职责单一性：... 职责完整性：... 目标一致性：... [技能选择] 必要性：...",
  "revised_states": [
    {
      "order_index": 0,
      "responsibility": "职责类型（如：信息收集、数据分析、内容生成、验证校对等）",
      "intent": "[职责类型]: 调整后的意图",
      "goal": "调整后的目标",
      "completion_criteria": [
        {"field": "字段名", "operator": ">=", "value": 1}
      ],
      "required_skill_names": [],
      "selected_skills": [
        {"skill_name": "技能名称", "relevance_reason": "选择原因"}
      ]
    }
  ],
  "replan_reason": "调整理由"
}
```
请保持调整后的阶段数量合理，确保最终仍能达成全局目标。

### 注意
- `responsibility`（必填）：每个 State 必须有明确的单一职责类型，禁止将多种职责混在一个 State 中。
- `required_skill_names` 是可选字段：只有当该 State 明确需要调用外部工具或加载技能资源时才填写具体的 skill_name；纯推理/对话/写作类任务应留空数组 `[]`。
- 不要因为"可能有用"而强选技能，只在有明确外部工具需求时才选择。
- 调整后的 State 仍然必须遵循"一职一态"原则，不能出现职责重叠。
- `selected_skills`（必填）：预分配的技能筛选结果。对于需要外部工具的 State，列出筛选出的技能及选择理由；纯推理/写作类 State 留空数组 `[]`。
