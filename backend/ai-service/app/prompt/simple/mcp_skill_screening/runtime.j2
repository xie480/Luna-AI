### 技能初筛详细指南

请严格按照以下步骤和方法执行 Skill 初筛决策：

#### Step 1：理解用户需求
- 分析【用户输入】中的核心需求，识别用户想要实现的具体目标。
- 参考【输入重构判定】中的判定结果：
  - `SKILL_NEED_TOOL`：是否需要技能。若为 `false`，直接判定 `no_suitable_skill=true`。
  - `SKILL_REASON`：判定原因，帮助理解为什么需要/不需要技能。
  - `SKILL_KEYWORDS`：检索关键词，辅助匹配技能的能力范围。
- 如果 `SKILL_NEED_TOOL` 为 `false`，无需执行后续步骤，直接输出无需技能的决策。

#### Step 2：逐一评估候选技能
对【候选技能列表】中的每个 Skill，从以下维度进行独立评估：

1. **能力覆盖度**：技能的名称和描述是否覆盖了用户需求的核心部分？是直接覆盖还是间接覆盖？
2. **精准匹配度**：技能描述中是否有明确的关键词与用户需求匹配（如"搜索"、"文件管理"、"代码分析"）？
3. **组合协作性**：如果单个技能不满足全部需求，哪些技能组合在一起可以覆盖完整需求链？
4. **成本代价**：如果多个技能都能满足需求，优先选择描述最精准、能力最聚焦的技能，避免选择过于宽泛的技能。

对每个 Skill 给出一个评分（满分为 5 分），并在 `reasoning` 中说明扣分原因。

#### Step 3：技能选择优先级规则

| 场景 | 选择策略 |
|------|----------|
| 单个技能完全覆盖用户需求 | `selected_skill_ids` 仅包含一个元素 |
| 多个技能组合可覆盖完整需求链 | 按执行顺序的优先级降序排列（先执行的在前） |
| 有高度重叠的技能 | 选择描述最精准、与被判定关键词最相关的技能 |
| 没有任何技能匹配 | `no_suitable_skill=true`，`selected_skill_ids` 设为空列表 |
| `SKILL_NEED_TOOL=false` | `no_suitable_skill=true`，`selected_skill_ids` 设为空列表 |

#### Step 4：组合约束
- `selected_skill_ids` **最多支持 3 个 Skill 组合**，禁止选择超过 3 个。
- 组合时，第一个 Skill 应为主技能（最核心需求的承担者），后序为辅助技能。
- 如果选择多个 Skill，必须在 `reasoning` 中明确说明：
  - 每个 Skill 负责解决用户需求的哪一部分。
  - 多个 Skill 之间的协作关系（串行还是并行）。

#### Step 5：最终校验
- 所有选中的 `skill_id` 必须严格来自【候选技能列表】，**绝对禁止虚构技能名称**。
- 如果 `no_suitable_skill=true`，`selected_skill_ids` **必须为空列表**。
- 如果 `selected_skill_ids` 为空列表但 `no_suitable_skill=false`，这个组合是无效的。

---

### ⚠️ 严格枚举约束与输出格式要求 ⚠️

你必须严格输出以下 JSON 结构。
【致命约束】：所有字段值必须严格遵循字段描述中的约束范围，禁止自行编造！
输出必须是一个合法的 JSON 对象，**禁止**包含任何额外的解释性文本、Markdown 代码块标记或注释。

```json
{
  "selected_skill_ids": [
    "选中的 Skill ID 列表，按优先级排序。\n每个元素必须是候选技能列表中的 skill_id 值。\n最多支持 3 个 Skill 组合。\n如果没有任何技能匹配或无需技能，此列表必须为空 []。"
  ],
  "reasoning": "初筛推理过程。详细说明：\n1. 用户核心需求是什么\n2. 为什么选择这些（或某个）Skill\n3. 每个 Skill 解决用户需求的哪一部分\n4. 如果选择了多个 Skill，说明它们的协作关系\n5. 如果没有任何技能匹配，说明原因和已做的尝试\n6. 如果 SKILL_NEED_TOOL=false，说明判定来源",
  "no_suitable_skill": "布尔值。\ntrue 表示候选技能中无任何技能匹配用户需求，或根据输入重构判定无需技能。\nfalse 表示至少选中了一个合适的技能。\n当此值为 true 时，selected_skill_ids 必须为 [](空列表)。"
}
```
