### 当前系统时间
-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

- 生成前推演流程：你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。
  - **信息密度校验**：压缩后是否保留了对后续对话可能有用的关键信息？有没有过度简化？
  - **融合自然性校验**：新旧摘要的融合是否自然连贯？有没有出现断层或矛盾？
  - **关键事实原子化校验**：key_facts 中的每条事实是否独立、明确、可直接用于后续对话？
  - **静态信息排除校验**：key_facts 中是否错误包含了已在 System Prompt 中存在的人设信息？

- 输出要求：
  - 只输出 JSON，不要输出任何解释、前言或多余文本。
  - JSON 必须包含三个字段（core_summary、key_facts、check）：
    - "core_summary"：更新后的核心摘要，要求高度浓缩但信息完整，保留整体脉络、上下文关联与长期重要信息。压缩时不得过度简化，需保证后续仅依靠该摘要仍能较好恢复对话背景。
    - "key_facts"：更新后的关键事实。仅用于捕获对话历史中新出现的关键事实、事件或动态信息。要求以条目化、原子化、可检索的方式呈现。
    - "check"：系统校验结果，格式为："[信息密度] ... [融合自然性] ... [关键事实原子化] ... [静态信息排除] ..."
  - 保持 key_facts 中每条事实尽量独立、明确、可直接用于后续对话。
  - key_facts 必须使用固定格式：
    "1.事实A;2.事实B;3.事实C"
    使用阿拉伯数字顺序编号，以英文分号（;）分隔，禁止使用数组、换行、项目符号或其他格式。
  - 【严格限制】：严禁在 key_facts 中提取、包含或重复任何已在 System Prompt 中存在的人设信息、角色背景或静态设定（例如：AI 的名字、性格特征、对用户的称呼等）。
  - core_summary 压缩后不得过短，应保留足够上下文信息与事件关联性，避免仅保留关键词或极度简写内容。
  - 不要编造未出现的信息；不要引入推测性内容；不要遗漏对后续对话可能有用的关键信息。
  - 若没有新增有效信息，仍需对现有内容进行整合与轻微压缩，提升表达稳定性、一致性和信息密度。