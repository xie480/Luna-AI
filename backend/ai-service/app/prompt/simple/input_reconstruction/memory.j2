### 当前短期会话上下文 (Short-Term Memory)
-当前会话背景开始-
核心梗概：
{{ CORE_SUMMARY }}
关键事实：
{{ KEY_FACTS }}
-当前会话背景结束-

-记忆片段开始-
{{ MEMORY_SNIPPETS }}
-记忆片段结束-

### 当前知识库文档
-知识库文档开始-
{{ KNOWLEDGE_DOCS }}
-知识库文档结束-

### 变量使用说明

本槽位提供以下四个变量，供你在解析用户输入时参考：

1. **CORE_SUMMARY（核心梗概）**
   - 当前会话的简短摘要，描述本轮对话的核心主题。
   - 用途：帮助你快速理解对话背景，避免重复询问已讨论过的话题。

2. **KEY_FACTS（关键事实）**
   - 当前会话中已确认的关键事实列表，每行一条。
   - 用途：当用户提及「刚才提到的那个配置」等指代时，请从此变量中查找具体事实。

3. **MEMORY_SNIPPETS（近期对话片段）**
   - 当前会话中最近数轮对话的原始文本片段，包含回复正文、内心独白及长回答摘要（若存在）。
   - 用途：感知最近的对话流，理解用户的即时上下文。
   - 注意：此变量仅包含近期消息，不包含完整历史。

4. **KNOWLEDGE_DOCS（知识库文档列表）**
   - 当前用户知识库中所有 ACTIVE 状态文档的名称与简介。
   - 用途：当用户提问与「文档」「资料」「知识库」「引用」相关的内容时，请依据此列表判断是否需要触发知识库检索；若用户的意图无法通过现有文档实现，则无需触发。