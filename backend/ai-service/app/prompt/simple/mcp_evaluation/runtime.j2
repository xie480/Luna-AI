### 执行结果评估详细指南

请严格按照以下步骤和方法对当前轮次的工具执行结果进行逐项评估：

#### Step 1：理解初始目标
- 认真阅读【用户初始意图】，明确用户真正想要的是什么。
- 阅读【当前步骤目标】，理解本步骤被设计出来要解决的具体问题。

#### Step 2：对比执行结果
- 仔细审查【工具执行结果】，逐条确认是否包含了与目标相关的核心信息。
- 判断结果类型：
  - **实质性数据**：结果中包含了回答用户问题所需的具体数据、事实或证据。
  - **部分信息**：结果只覆盖了问题的一部分子集，但核心问题仍缺少关键支撑。
  - **无关噪音**：结果与用户意图完全不相关，或者是错误的、报错的输出。
  - **无结果**：工具正常返回，但结果为空。

#### Step 3：目标达成判定

| 条件 | 判定 | 说明 |
|------|------|------|
| 结果中包含足以回答核心问题的实质性信息 | `is_met=true` | 即使数据格式不完美、信息量不完整，也可以判定达成 |
| 结果部分覆盖，但有明确的信息缺口 | `is_met=false` | 需要在 suggestion 中准确描述缺口 |
| 结果完全无关或报错 | `is_met=false` | 需要在 suggestion 中给出调整策略 |
| 工具返回空结果 | `is_met=false` | 需要在 suggestion 中建议调整输入参数 |

#### Step 4：生成调整建议（仅当 is_met=false 时）
- `suggestion` 必须是对下一轮执行有切实指导意义的策略调整建议。
- 好的建议应当包含具体操作，例如建议调整哪些参数、换用什么输入、或换用什么方向。
- 糟糕的建议是笼统的"继续执行"，没有给出任何可操作的新方向。

---

### ⚠️ 严格枚举约束与输出格式要求 ⚠️

你必须严格输出以下 JSON 结构，必须合法。
【致命约束】：`is_met` 必须严格遵循上述 Step 3 的规则，禁止随意判定 true 或 false。
输出必须是一个合法的 JSON 对象，**禁止**包含任何额外的解释性文本、Markdown 代码块标记或注释。

```json
{
  "is_met": "布尔值。true 表示目标已达成，当前轮次的工具调用收集到的信息已经足以回答用户的核心问题。false 表示信息仍不充足，需要继续下一轮执行或调整策略。",
  "suggestion": "字符串。仅当 is_met=false 时必填。给出具体的、可操作的下一轮策略调整建议。建议应包含明确的动作方向。当 is_met=true 时此字段可以为空字符串。"
}
```
