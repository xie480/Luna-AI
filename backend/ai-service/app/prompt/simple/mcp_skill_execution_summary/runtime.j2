### 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

#### C1：结果有效性校验
1. **过滤无用结果**：是否正确过滤了未产生有效结果或结果完全无用的技能？
2. **关键事实提取**：key_facts 是否准确提取了每个技能执行结果中的核心信息？

#### C2：跨轮次去重校验
1. **冗余去除**：相同技能的多次执行输出是否已合并去重？
2. **信息完整性**：合并后的结果是否保留了各轮次中的关键信息，没有丢失？

【输出格式约束】
你必须返回一个 JSON 对象，包含 results、check 和跨轮次合并后的技能执行摘要。格式如下：
```json
{
  "check": "[结果有效性] 过滤无用结果：... 关键事实提取：... [跨轮次去重] 冗余去除：... 信息完整性：...",
  "results": [
    {
      "skill_name": "技能名称（例如 WebSearch, FileRead）",
      "result_summary": "一句话总结该技能的最终执行结果（例如：成功获取了关于 XX 的搜索结果 / 读取了 YY 文件的内容）。",
      "key_facts": [
        "提取出的关键事实 1",
        "提取出的关键事实 2"
      ]
    }
  ]
}
```
注意：如果某个技能未产生有效结果，或其结果完全是无用信息，请直接将其忽略，不要包含在 results 数组中。

请跨轮次综合分析，去除冗余结果，合并相同技能的多次执行输出。
严格按照上述 JSON 格式输出，不要包含任何额外说明文字。