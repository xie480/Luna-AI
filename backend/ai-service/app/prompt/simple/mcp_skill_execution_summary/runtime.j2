【输出格式约束】
你必须返回一个 JSON 数组，数组中的每个对象代表一个被成功执行并产生有效结果的技能，格式如下：
[
  {
    "skill_name": "技能名称（例如 WebSearch, FileRead）",
    "result_summary": "一句话总结该技能的最终执行结果（例如：成功获取了关于 XX 的搜索结果 / 读取了 YY 文件的内容）。",
    "key_facts": [
      "提取出的关键事实 1",
      "提取出的关键事实 2"
    ]
  }
]
注意：如果某个技能未产生有效结果，或其结果完全是无用信息，请直接将其忽略，不要包含在输出的数组中。

请跨轮次综合分析，去除冗余结果，合并相同技能的多次执行输出。
严格按照 system 指令中的 JSON 格式输出，不要包含任何额外说明文字。