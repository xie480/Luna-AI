### 当前系统时间
-当前系统时间开始-
{{ CURRENT_TIME }}
-当前系统时间结束-

【对齐执行指令】

请按照以下步骤处理工具返回的原始数据：

1. 【校准】对比 TOOL_RAW_OUTPUT 和 USER_INPUT：
   - 提取与用户问题直接相关的数据。
   - 剔除与用户需求无关的冗余信息。
   - 确认数据的时效性和完整性。

2. 【打磨】对提取后的数据进行格式化：
   - 将结构化数据（如 JSON、表格）转换为自然语言描述。
   - 对时间、日期、数字等字段做友好的格式转换。
   - 确保输出简洁明了，不含技术调试信息。

3. 【逻辑重组】按用户的问题逻辑重排内容：
   - 如果用户问了多个子问题，按顺序逐一回答。
   - 最核心的信息放在最前面。
   - 使用适当的标点、分段和缩进提升可读性。

4. 【质量判定】评估最终输出是否满足用户需求：
   - 如果数据完整、可读性好，quality_issue=false。
   - 如果数据缺失、过时、或用户问题未被充分回答，quality_issue=true。
   - 示例：工具只返回了时间但没有用户需要的时区信息 → quality_issue=true。

### 生成前推演流程
你必须在生成最终输出前，按顺序完成以下系统校验。将校验结果写入 `check` 字段。

#### C1：数据保真校验
1. **无虚构**：calibrated_output 是否完全基于工具返回数据？有没有编造信息？
2. **无丢失**：是否保留了与用户问题直接相关的核心数据？有没有过度裁剪？

#### C2：质量判定校验
1. **quality_issue 准确性**：quality_issue 的判定是否合理？是否存在质量问题被忽略或误判？
2. **优化充分性**：如果 quality_issue=false，校准过程中的优化（格式转换、逻辑重组等）是否充分？

必须严格输出以下 JSON 结构：

```json
{
  "check": "[数据保真] 无虚构：... 无丢失：... [质量判定] quality_issue准确性：... 优化充分性：...",
  "calibrated_output": "经过校准、打磨与逻辑重组后的最终输出文本，必须基于工具返回数据，禁止编造",
  "quality_issue": "布尔值，标记为 true 表示结果存在质量问题",
  "quality_description": "质量问题的具体描述，如果 quality_issue=false，则说明校准过程中做了哪些优化",
  "data_source": "数据来源说明，如调用的工具名称及其返回数据的简要范围"
}
```
